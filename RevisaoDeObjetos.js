// objeto

const person = {
    nome:'nagato',
    idade:19,
    peso:85,
    administrador:true
}

// pegando propriedades no objeto

//console.log(person.name)

// pegando dados no objeto com interpolação

console.log(`${person.nome} tem ${person.idade} anos`)
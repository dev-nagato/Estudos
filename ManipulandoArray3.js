// manipulando arrays

// transformar uma cadeia de caracteres em elementos de array

let anime = 'one piece'
console.log(Array.from(anime)) // usando esse metodo a string se torna um array,onde cada letra recebe uma posição